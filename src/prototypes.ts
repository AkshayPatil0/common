export enum UserRoles{
	user= "user",
	vendor = "vendor",
	admin = "admin"
}

export enum OrderStatus{
	cart= "cart",
	created= "created",
	awaitingPayment = "awaiting-payment",
	paid = "paid",
	cancelled = "cancelled",
	completed = "completed"
}