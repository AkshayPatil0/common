export enum UserRoles{
	user= "user",
	vendor = "vendor",
	admin = "admin"
}

export enum OrderStatus{
	created= "created",
	awaitingPayment = "awaiting-payment",
	paid = "paid",
	cancelled = "cancelled",
	completed = "completed"
}